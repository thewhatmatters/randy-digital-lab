<!-- Backend build notes — ONE WRITER: the backend builder. Appended per
     round: what was built, load-bearing design decisions with
     consequence-based reasons, flagged ambiguities, one proposed
     simplification. Everything outside HTML comments is written back to
     the tracker byte-for-byte. -->
