<!-- Frontend build notes — ONE WRITER: the frontend builder. Appended per
     round: what was built, key choices, anything the AC left ambiguous.
     Builders never render-check their own work. Everything outside HTML
     comments is written back to the tracker byte-for-byte. -->
